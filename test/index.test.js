// index.test.js

const { regex } = require('./../lib/index');

test('index.js exports the regex object correctly', () => {
  // Check if regex is an object
  expect(typeof regex).toBe('object');


});